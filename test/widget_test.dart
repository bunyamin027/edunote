import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build without errors', (WidgetTester tester) async {
    // Basic build test — will be expanded with proper widget tests
    expect(true, isTrue);
  });
}
